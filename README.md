# robotframework-NestedLogger
The goal of the library is to enable the registration of keywords so that they are visible at the HTML level as individual keywords, while their implementation is nested in python.
